interface DisplayOptionsProps {
}
const DisplayOptions = ({}:DisplayOptionsProps) => {
    return (
        <div>
                DisplayOptions
        </div>
    );
};

export default DisplayOptions;